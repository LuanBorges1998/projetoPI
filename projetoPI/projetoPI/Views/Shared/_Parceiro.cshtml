@model IEnumerable<projetoPI.Models.parceiro>

<p>
    @Html.ActionLink("Adicionar Parceiro", "AdicionaParceiro", "SocioColaborador", null, new { @class = "links" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "EditParceiro", new { id=item.id }, new { @class = "links" }) |
            @Html.ActionLink("Excluir", "DeleteParceiro", new { id=item.id }, new { @class = "links" })
        </td>
    </tr>
}

</table>
